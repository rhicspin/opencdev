language: cpp
before_script:
  - sudo apt-get update -qq
  - sudo apt-get install -qq libboost-dev libboost-filesystem-dev libboost-test-dev
  - mkdir build
  - cd build
  - cmake ..
script:
  - make
  - make test
